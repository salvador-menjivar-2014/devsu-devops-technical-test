# k8s/service.yml
apiVersion: v1
kind: Service
metadata:
  name: devsu-api-service
spec:
  type: ClusterIP
  selector:
    app: devsu-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000